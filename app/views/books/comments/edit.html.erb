<h1><%= t('views.common.title_edit', name: Comment.model_name.human.downcase) %></h1>

<%= render 'books/comments/form', commentable: @commentable, comment: @comment %>

<br>

<nav class="page-nav">
  <%= link_to t('views.common.show', name: Comment.model_name.human.downcase), @commentable %> |
  <%= link_to t('views.common.back', name: i18n_pluralize(Comment.model_name.human.downcase)), @commentable %>
</nav>
